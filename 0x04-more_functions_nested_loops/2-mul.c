#include "main.h"

/**
 * mul - function to multiply two intergers
 *
 * @a: 1st interger to be multiplied
 * @b: 2nd interger operand to be multiplied
 * Return: interger result of a*b.
 */
int mul(int a, int b)
{
	int mult = 0;

	mult = a * b;

	return (mult);
}
