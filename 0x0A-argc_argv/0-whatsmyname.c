#include <stdio.h>
/**
 *  - function that prints the name of the program
 * Return: always zero sgnifyin
 */
int main(__attribute__((unused)) int argc,char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
