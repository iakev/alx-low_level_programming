#include "main.h"

/**
 * add - check the code
 * @a: firsrt operand to be added
 * @b: second operand to be added to first operand
 * Return: a+b .
 */
int add(int a, int b)
{
	int result = 0;

	result = a + b;
	return (result);
}
