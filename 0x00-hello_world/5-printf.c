#include<stdio.h>
/**
 * main - Entry point
 * program to print a specified string followed by a new line using printf
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
